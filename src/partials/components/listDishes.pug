mixin listDishes(dishes)
    ul.listDishes
        each dish in dishes
            li.restaurant__menu--composition
                h4=dish.name
                .restaurant__menu--description
                    span.restaurant__menu--text=dish.description 
                    p=dish.price
